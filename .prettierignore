# Build output and generated code — formatting these is noise, and Prettier
# rewriting a generated file is a diff nobody asked for.
dist
.output
.netlify
.vinxi
coverage
build

# Prisma's generated client and migration SQL.
apps/api/prisma/generated
**/*.sql

# Playwright's own output. `test-results` is emptied every run.
playwright-report
test-results

# Lockfiles and package manifests keep their tool's own formatting.
package-lock.json

# The routeTree is generated by TanStack Router on every dev start.
apps/web/src/routeTree.gen.ts

# Retired code kept for the record only (see D43). Reformatting it would
# rewrite history that is deliberately frozen.
docs/archive
